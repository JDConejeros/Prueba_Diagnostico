library(surveydown)

# Ejecuta este script una sola vez (o cuando quieras cambiar credenciales).
# Te pedirá en consola los parámetros de conexión de Supabase
# (usar "Transaction Pooler" en la pantalla Connect de Supabase).
sd_db_config()

cat("\nArchivo .env creado/actualizado correctamente.\n")
